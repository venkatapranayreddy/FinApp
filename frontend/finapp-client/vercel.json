{
  "buildCommand": "ng build --configuration production",
  "outputDirectory": "dist/finapp-client/browser",
  "framework": "angular",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
